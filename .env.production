# File ini disalin oleh Dockerfile (COPY .env.production ./.env) saat build image.
# Nilai konkret diberikan saat runtime melalui docker-compose `environment:` block,
# sehingga di sini cukup default aman saja.

NODE_ENV=production
PORT=3000
